# Файлы tags.tsv и tags_test.tsv будут перезаписаны!
# Используется модель https://storage.googleapis.com/download.tensorflow.org/models/inception5h.zip

# Для загрузки ранее сохранённой модели раскомментировать:
#LoadModel = model.zip

# Иначе:
# Создание модели - скопируйте примеры для распознавания в соответствующую структуру каталогов (можно на русском) в папке assets/images
# Количество попыток анализа изображений - будет выбран лучший вариант
TryCount = 10
# Полученная модель будет сохранена, в следующий раз ееё можно загрузить - это быстрее
SaveModel = model.zip

# Путь для поиска изображений для классификации - они будут скопированы в соответствующие разделы папки assets/result
SourseImages = E:\_Heap

# Сколько изображений найти (скопировать) и остановиться.
# Если название категории (имя папки) начинается с символа '_', изображения в этой категории не учитываются этим счетчиком
ResultImagesCount = 50

# Порог точности оценки для принятия решения о копировании (0 - 1), чем ближе к 1, тем больше нужна уверенность что изображение опознано верно
MaxScoreLimit = 0.97